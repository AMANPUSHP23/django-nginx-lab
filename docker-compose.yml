services:

  db:
    image: postgres:16
    container_name: postgres-db

    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: django
      POSTGRES_PASSWORD: django123

    volumes:
      - postgres_data:/var/lib/postgresql/data

  web:
    image: amanpushp/django-nginx-lab:latest
    container_name: django-web

    command: sh -c "sleep 15 && python manage.py migrate && gunicorn myapp.wsgi:application --bind 0.0.0.0:8000"

    depends_on:
      - db

  nginx:
    image: nginx:latest
    container_name: nginx

    ports:
      - "80:80"

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf

    depends_on:
      - web

volumes:
  postgres_data:
